#Using for loop:
tuple1 = ("csk ", "in", "yellow")
for i in range(len(thistuple)):
  print(thistuple[i])

#Using while loop:
tuple2 = ("chrome", "microsoft", "edge")
i = 0
while i < len(thistuple):
  print(thistuple[i])
  i = i + 1
  
